<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/layout_settings"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="top"
        android:padding="0dp"
        style="@style/Theme.settings">

        <LinearLayout
            android:id="@+id/layout_panel_header"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:orientation="horizontal"
            android:gravity="top">

            <LinearLayout
                android:id="@+id/layout_title"
                android:orientation="horizontal"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="match_parent"
                android:gravity="top">

                <ImageButton
                    android:id="@+id/btn_arrow_back"
                    style="@style/settings_btn_back">
                </ImageButton>

                <TextView
                    android:id="@+id/text_view_title"
                    android:text="@string/btn_settings"
                    style="@style/title_other">
                </TextView>

            </LinearLayout>

            <Button android:id="@+id/btn_filter_list"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:gravity="right|center"
                android:layout_margin="4dp"
                android:visibility="invisible">
            </Button>

        </LinearLayout>

        <LinearLayout android:id="@+id/layout_theme_select"
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="61dp"
            android:gravity="top"
            android:layout_marginTop="24dp">

            <TextView
                android:id="@+id/text_view_theme_select"
                style="@style/settings_item"
                android:text="@string/theme_select">
            </TextView>

            <Switch
                android:id="@+id/switch_theme_select"
                style="@style/Theme.settings_item_switch"
                android:layout_width="wrap_content"
                android:layout_height="18dp"
                android:layout_gravity="start|center_vertical"
                android:layout_marginEnd="12dp"
                android:layout_marginStart="0dp">
            </Switch>
        </LinearLayout>

        <LinearLayout android:id="@+id/layout_share"
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="61dp"
            android:gravity="top">

            <TextView
                android:id="@+id/text_view_share"
                style="@style/settings_item"
                android:text="@string/str_share_app">
            </TextView>

            <ImageButton
                android:id="@+id/btn_share"
                style="@style/settings_item_icon"
                android:src="@drawable/share">
            </ImageButton>
        </LinearLayout>

        <LinearLayout android:id="@+id/layout_support"
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="61dp"
            android:gravity="top">

            <TextView
                android:id="@+id/text_view_support"
                style="@style/settings_item"
                android:text="@string/str_ask_support">
            </TextView>

            <ImageButton
                android:id="@+id/btn_support"
                style="@style/settings_item_icon"
                android:src="@drawable/support">
            </ImageButton>
        </LinearLayout>

        <LinearLayout android:id="@+id/layout_agreement"
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="61dp"
            android:gravity="top">

            <TextView
                android:id="@+id/text_view_agreement"
                style="@style/settings_item"
                android:text="@string/str_user_agreement">
            </TextView>

            <ImageButton
                android:id="@+id/btn_agreement"
                style="@style/settings_item_icon"
                android:src="@drawable/arrow_forward">
            </ImageButton>
        </LinearLayout>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>